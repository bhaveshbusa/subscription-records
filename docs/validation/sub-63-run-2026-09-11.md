# SUB-63 evaluation — 11 September 2026

Status: started; no human journey has passed yet. Follow [the validation guide](../testing-and-signoff.md). SUB-64 is excluded.

## Starting conditions

- Evaluator: Bhavesh.
- App: http://localhost:3000.
- Account: no subscriptions, confirmed by Bhavesh before the first task.
- Dataset reset: none performed by the guide.
- Local source checkout: `019e942`, containing merged SUB-65; clean working files at the initial check. The running server's exact source revision has not been independently established.
- Reachability: HTTP 307 observed at the app root on 11 September 2026, 21:42 UTC. This establishes reachability, not successful sign-in or functional behaviour.
- Browser/device and live extraction channels: not yet recorded.

## First visit

### J1 — Start with remembered names

Task: start the inventory with three to five services remembered without researching details; save what is known using the user's own navigation.

Outcome at setup: Not run. The 12 September feedback below provides partial observations; the full J1 checklist is not yet verified.

Record approximate time to first saved subscription, whether the result matches the supplied names, unknown details, and any confusion or assistance. Do not record private source contents.

## Remaining evaluation

J2–J7, live channel checks, both later return visits and human sign-off are pending. The preparation PR's automated results do not count as these observations.

## Feedback received 12 September 2026

These are Bhavesh's observations from the initial evaluation, not an independently replayed browser test or evidence of a separate return visit. The source checkout still reads `019e942`; the running process revision and live-versus-fixture extractor mode remain unverified. No fine UI polish assessment was requested.

### What worked and what felt costly

- Actions and information close to the subscription felt more natural; Bhavesh judged the UX much better than the previous version.
- Confirming each field felt like extra work. Record this as review-effort friction, not consent to automatic application. Explore an explicit action to confirm a reviewed set of changes while retaining individual correction and trust controls; no new interaction is approved or implemented here.
- J1 has partial user-reported evidence of creating proposals/subscriptions. Time to first saved record, exact saved counts, rejection and bulk/trial-list checks have not been reported; do not mark the whole journey passed.

### F1 — Selected context rejects ordinary billing text (reported failure)

Warp: after adding the subscription, a provider-free excerpt containing an annual price, trial end and the same next billing date was rejected until Bhavesh repeated the provider name.

ChatGPT: after creating the subscription, an excerpt beginning “Your plan auto-renews” with a dated renewal was rejected as mentioning “Your plan” rather than ChatGPT.

Expected: an excerpt without a different service name should use the explicitly selected subscription. A genuinely different service or incompatible account must still be clarified. All resulting changes remain proposals.

Code inspection: `app/api/chat/route.ts` first calls `extractCandidates(text)` without the target; it only prefixes the selected provider and retries if no candidate has a non-empty provider. `lib/capture/target.ts` rejects unequal canonical provider strings. The observed “Your plan” error is consistent with a generic phrase being treated as a provider; exact extractor output has not been inspected. Track text-context repair separately from this documentation PR.

### F2 — Invoice enrichment splits the intended Claude draft (reported failure)

Bhavesh created a Claude proposal, uploaded an invoice in that context, saw Claude Pro treated as provider rather than Claude plus plan Pro, corrected provider/plan and ended with two proposals: the original and the invoice-derived proposal.

Expected: enrichment of the same intended pending holding stays on that draft and preserves evidence, including through provider/plan correction. Distinct holdings/accounts must not merge.

Code inspection: `lib/capture/file-capture.ts` falls back to all-subscriptions capture when provider matching fails, clearing the pinned holding, question and draft-to-revise. This can explain loss of draft context if extraction names Claude Pro while Claude is selected. It is a plausible cause, not a reproduced transaction trace. Provider/plan separation and draft identity need a synthetic reproduction in their own repair PR.

### F3 — Invoice amount basis and missing renewal (awaiting matching evidence)

Bhavesh reported that the Claude invoice produced a pre-tax subscription amount and no next renewal. The attached reference was inspected and is a Cursor invoice, not Claude, so it cannot establish what the Claude document stated. Do not commit the invoice, personal details, identifiers or monetary contents.

Invoice interpretation must distinguish recurring service cost, tax and one-off credits/balance adjustments rather than equate amount due with recurring cost. The current amount contract does not explicitly settle tax treatment. Obtain the matching evidence and clarify the intended cost basis before any monetary behaviour change. Invoice issue/due dates and service-period end are not automatically a confirmed next renewal.

### F4 — Warp date expectation (resolved by user clarification)

Bhavesh initially reported no next renewal after the provider-prefixed excerpt. He then confirmed that status was Trial and the trial-end date was set correctly. This specific trial interpretation check passes on his report.

The current trial contract records trial end separately as the expected payment-start boundary and does not invent a recurring next renewal from it. The extractor prompt requests `trialEndsOn`; the record writer excludes recurring renewal updates for Trial rows. The blank recurring next-renewal field is consistent with this contract, so it is not filed as a date-capture defect. The initial expectation mismatch may inform future wording; no finer UI change is required here. Requiring the provider prefix remains F1.

### Validation status

Contextual capture and draft consolidation have reported failures; review has reported friction. No whole journey is marked passed from this feedback. The Warp trial-date question is resolved; matching Claude invoice evidence remains pending. Both actual return visits, live-mode confirmation and final human sign-off remain outstanding. SUB-64 is excluded.

## Linked repairs

- [SUB-66](https://linear.app/lets-play-match/issue/SUB-66/use-the-selected-subscription-when-billing-excerpts-omit-the-provider): provider-free excerpts in selected context (F1).
- [SUB-67](https://linear.app/lets-play-match/issue/SUB-67/keep-invoice-enrichment-on-the-selected-draft-through-provider-and): provider/plan correction and invoice draft continuity (F2).

Both are Backlog repair issues linked to SUB-63, with synthetic reproduction and live retest requirements. Neither repair has been implemented in the validation PR.
