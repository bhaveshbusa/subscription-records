import { FoundationsGallery } from "@/components/ui/foundations-gallery";

/** Synthetic, data-free review fixture for SUB-72. Not linked from the product. */
export default function UiFoundationsPage() {
  return <FoundationsGallery />;
}
