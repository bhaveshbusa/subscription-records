# CLAUDE.md

Read **[AGENTS.md](AGENTS.md) first.** It is the contract every coding agent
works to: where "correct" is defined, the stack, repo conventions, domain
rules, and the definition of done.

Nothing project-specific lives in this file. Only Claude Code reads it, so
anything written only here would be invisible to the other agents that work on
this repo — if you find yourself wanting to add a rule, put it in
[AGENTS.md](AGENTS.md) or the `docs/` file it belongs to instead.

[README.md](README.md) covers running the app, the API surface, and how the
test database works.
